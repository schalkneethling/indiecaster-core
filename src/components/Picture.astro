---
const {
  alt = "",
  height = "200",
  loading = "lazy",
  pictureSrc,
  width = "200",
} = Astro.props;
---

<picture>
  <source srcset={`${pictureSrc}.avif`} type="image/avif" />
  <img
    alt={alt}
    class="hero-image"
    width={width}
    height={height}
    loading={loading}
    src={`${pictureSrc}.webp`}
  />
</picture>
